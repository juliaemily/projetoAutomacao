{
  "name": "template-cypress",
  "version": "1.0.0",
  "description": "template-cypress",
  "main": "index.js",
  "scripts": {
    "test-open": "npx cypress open",
    "test-headless": "npx cypress run",
    "test-mobile": "npx cypress run --config viewportWidth=410 viewportHeight=860"
  },
  "dependencies": {
    "cypress": "^14.0.3"
  },
  "devDependencies": {
    "mocha": "^11.1.0"
  }
}
